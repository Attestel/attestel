# Union of the four Python services' pinned deps (analysis + llm + prediction + events).
# Versions are identical across services where they overlap, so there are no conflicts.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
requests==2.32.3
numpy==2.2.1
pandas==2.2.3
yfinance==1.5.1
scikit-learn==1.5.2
lightgbm==4.5.0
